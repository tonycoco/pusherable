language: ruby
script: "bundle install && bundle exec rake"
rvm:
  - 1.9.2
  - 1.9.3
  - 2.0.0
env:
  - DB=mongo
  - DB=sqlite3
before_script:
  - mongo pusherable_test --eval 'db.addUser("travis", "test");'
